// import * as HoursAPIUtil from "../util/hours_api";

// export const RECEIVE_HOURS = "RECEIVE_HOURS";

// const receiveHours = hours => {
//     return {
//         type: RECEIVE_HOURS,
//         hours
//     };
// };

// export const getHours = (query) => dispatch => (
//     HoursAPIUtil.searchHours(query)
//         .then(hours => dispatch(receiveHours(hours)))
// );